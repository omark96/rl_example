type Color* = struct {r, g, b, a: uint8}
type TextureHandle* = uint

fn drawText*(text: str, posX, posY, fontSize: int32, color: Color)
fn drawRectangle*(posX, posY, width, height: int32, color: Color)
fn loadTexture*(filename: str): TextureHandle
fn drawTexture*(textureHandle: TextureHandle, x, y: int32, tint: Color)